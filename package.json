{
  "name": "ouvidoria-painel",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply ouvidoria-painel --local",
    "db:migrate:remote": "wrangler d1 migrations apply ouvidoria-painel --remote"
  },
  "devDependencies": {
    "wrangler": "^4.127.0"
  }
}
